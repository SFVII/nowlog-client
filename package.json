{
  "name": "nowlog-client",
  "version": "1.0.0",
  "description": "logger for nowlab apps through rabbitMQ",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist/*",
    "lint": "eslint . --ext .ts",
    "tsc": "tsc",
    "build": "npm-run-all clean lint tsc"
  },
  "files": [
    "/dist"
  ],
  "dependencies": {
    "amqplib": "^0.8.0",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^16.4.11",
    "@typescript-eslint/eslint-plugin": "^4.29.0",
    "@typescript-eslint/parser": "^4.29.0",
    "eslint": "^7.32.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
